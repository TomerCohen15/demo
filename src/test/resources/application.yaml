spring:
  application:
    name: demo
  datasource:
    username: yourusername
    password: yourpassword
    url: jdbc:postgresql://localhost:1234/postgres
  jpa:
    hibernate:
      ddl-auto: update