version: '2'

services:
  db:
    image: 'postgres:12.22-alpine3.21'
    container_name: db
    environment:
      - POSTGRES_USER=yourusername
      - POSTGRES_PASSWORD=yourpassword
    ports:
      - "1234:5432"